name: alco_market_models
description: Models for AlcoMarket
version: 0.0.1
homepage:

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  freezed: ^1.1.1
  freezed_annotation: ^1.1.0
  json_annotation: ^4.4.0
    

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.7
  flutter_lints: ^1.0.0
  json_serializable: ^6.1.4

flutter:
